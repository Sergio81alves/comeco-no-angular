import { Injectable } from '@angular/core';
import { Produtos } from '../Produtos';
import { HttpClient } from '@angular/common/http';
import { observable } from 'rxjs';

@Injectable({
  providedIn: 'root'
})
export class AdicionandoService {

  constructor() { }
}
